import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import useInput from'../hooks/useInput';import useAsync from'../hooks/useAsync';import{useApp}from'../contexts/AppContext';import{addNote}from'../utils/network-data';const TITLE_LIMIT=50;
export default function AddPage({notify}){const[title,onTitleChange]=useInput('');const[body,setBody]=useState('');const[error,setError]=useState('');const{loading,run}=useAsync();const{t}=useApp();const navigate=useNavigate();async function onSubmit(e){e.preventDefault();if(!title.trim()||!body.trim())return setError(t.required);try{await run(async()=>{const r=await addNote({title:title.trim(),body:body.trim()});if(r.error)throw new Error(r.message);notify(t.created);navigate('/')})}catch(err){setError(err.message)}}return <section className="editor-page page-container"><Link className="back-link" to="/">← {t.back}</Link><form className="editor-card" onSubmit={onSubmit}><span className="eyebrow">NEW NOTE</span><input className="title-input" value={title} onChange={onTitleChange} maxLength={TITLE_LIMIT} placeholder={t.title} autoFocus/><span className="counter">{TITLE_LIMIT-title.length} {t.charLeft}</span><textarea value={body} onChange={e=>setBody(e.target.value)} placeholder={t.body} rows="14"/>{error&&<div className="form-error">{error}</div>}<div className="editor-actions"><Link className="secondary-button" to="/">{t.back}</Link><button className="primary-button" disabled={loading}>{loading?t.loading:t.save}</button></div></form></section>}
